let msg = "you are so grounded mr.";
let caps = msg.toUpperCase();
console.log(caps);

let color = "  purple   ";
color.trim(); //returns a string by removing spaces from front and back
color.trim().toUpperCase();//first trim() is executed and then returned value is then uppercased